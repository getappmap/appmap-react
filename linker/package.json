{
  "name": "appmap-link",
  "private": true,
  "version": "0.0.1",
  "description": "Joins frontend interaction AppMaps to backend request AppMaps via W3C Trace Context ids, and renders stitched PlantUML sequence diagrams.",
  "type": "module",
  "bin": {
    "appmap-link": "bin/appmap-link.mjs",
    "appmap-simulate-backend": "bin/simulate-backend.mjs"
  },
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^3.1.3"
  }
}

{
  "name": "funwithappmap-deno",
  "private": true,
  "version": "0.0.1",
  "description": "Deno session driver: per-request AppMap recording for Deno.serve / Supabase edge functions.",
  "bin": {
    "appmap-deno": "bin/appmap-deno.ts"
  },
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "@appland/appmap-validate": "^2.5.1",
    "vitest": "^3.1.3"
  }
}
